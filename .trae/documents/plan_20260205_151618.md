我已经分析了需求并制定了详细的技术方案，特别是针对 10MB 大文本的性能优化。

方案已记录在 `d:\code\Android\MyApplication3\design_proposal.md` 文件中。

**核心策略：**

1. **自定义 View + 虚拟化渲染**：不使用 TextView，避免内存溢出和卡顿。
2. **后台预计算排版**：在后台线程计算好所有行的位置，主线程只负责绘制可见区域。
3. **高效索引**：建立 时间 <-> 单词 <-> 屏幕坐标 的映射索引，实现毫秒级的高亮和跳转。

**下一步：**
如果您对 `design_proposal.md` 中的方案没有异议，我将按照以下步骤进行编码：

1. 实现 `SubtitleSyncView` 的核心数据处理逻辑（文本测量与分行）。
2. 实现 `onDraw` 渲染逻辑（可见区域绘制、高亮绘制）。
3. 实现交互逻辑（点击跳转、自动滚动）。
4. 在 `MainActivity` 中集成并测试。

请确认是否开始编码？

先输出任务队列，要实现的功能不如，滑动，预加载之类的
